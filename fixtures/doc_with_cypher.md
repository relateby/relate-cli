# Query Examples

This document contains example Cypher queries for working with a graph database.

## Getting Started

Before running queries, ensure your Neo4j instance is running and accessible.

## Find a Person

The following query finds a person by name:

```cypher
MATCH (n
RETURN n
```

Replace `n` with your actual node pattern.
